import React from 'react';
import {Container} from './styles/MovieCardElements';

const MovieCard = ({ movie }) => {
  return (
    <Container>
     
    </Container>
  );
};

export default MovieCard;
